package com.squashedbuggames.json;

import java.util.ArrayList;
import java.util.Date;
import java.util.List;

public class Flights {
/*
    public List<Flight> setFlights (Passenger passenger, Ticket ticket){
        Passenger passenger = new Passenger("George", "Lannisburg");
        Ticket ticket = new Ticket(passenger, 1123);
        List<Ticket> ticketList = List.of(ticket);
        Flight newFlight = new Flight(ticketList, new Date());
      List <Flight> flightsList = new ArrayList<Flight>();
        flightsList.add(newFlight);
        return flightsList;
    }
*/
}
